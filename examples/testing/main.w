static success = 0
fn is(c m){
    if(c success m)
}

test multiplication {
    is(==(4 *(2 2)) "2 * 2 should be 4")
    is(==(1 *(1 1)) "1 * 1 should be 2")
}

test multiplication {
    is(==(5 +(2 2)) "2 * 2 should be 4")
    is(==(2 +(1 1)) "1 * 1 should be 2")
}
